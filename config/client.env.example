# Flutter 客户端环境变量参考（编译时 `--dart-define` 注入）

```text
API_BASE_URL=https://api.lcxxingyu.fun
```

| 场景 | 地址 |
|------|------|
| 生产（推荐） | `https://api.lcxxingyu.fun` |
| VPC 内网直连 | `http://172.25.19.38:8000` |
| 本机开发 | `http://127.0.0.1:9000` |
| Android 模拟器 → 本机 | `http://10.0.2.2:9000` |

Release 未传 `API_BASE_URL` 时，客户端默认使用 `https://api.lcxxingyu.fun`。

快捷构建：`stday/build_release_server.bat` / `stday/build_release_android.bat`

手动：

```powershell
flutter build windows --release --dart-define=API_BASE_URL=https://api.lcxxingyu.fun
flutter build apk --release --dart-define=API_BASE_URL=https://api.lcxxingyu.fun
```
