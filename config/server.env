# =============================================================================
# 部署服务器地址（当前生产/测试环境）
# =============================================================================

# 公网 IP — 外网客户端、Windows 本地开发机打包时用这个
SERVER_PUBLIC_IP=39.106.134.222
API_PUBLIC_URL=http://39.106.134.222:8000

# 私有 IP — 同一 VPC / 内网其他机器访问时用这个
SERVER_PRIVATE_IP=172.25.19.38
API_PRIVATE_URL=http://172.25.19.38:8000

# Flutter 打包推荐（外网分发）
API_BASE_URL=http://39.106.134.222:8000

# 后端监听（服务器本机，勿改）
UVICORN_HOST=0.0.0.0
UVICORN_PORT=8000

# PostgreSQL 仅本机访问，不对外开放
DATABASE_HOST=127.0.0.1
DATABASE_PORT=5432
